- ex seconds 以秒为单位设置过期时间
- px xxx 以毫秒为单位设置过期时间 

# 数值加减

- INCR key 递增数字
- INCRBY key increament 增加指的整数
- DECR key 递减数值
- DECRBY key decrement 减少指定长度

# 获取字符串长度
- STRLEN key 获取长度
- APPEND key value 追加

# 分布式锁
- setnx key value

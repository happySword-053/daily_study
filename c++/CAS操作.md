**CAS（Compare-And-Swap）循环**，又称为**自旋 CAS 循环**（CAS Spin Loop），是利用原子比较并交换操作来实现无锁（lock-free）数据结构或算法的一种常见模式。其核心思想是：

1. **读取**目标内存的当前值；
    
2. 计算出“想要写入”的新值；
    
3. 使用原子 CAS 操作尝试将当前值替换为新值；
    
4. 如果 CAS 成功，则完成操作；如果 CAS 失败（说明在此期间其它线程已经修改了该值），就重新回到第 1 步，重复尝试，直到成功为止。